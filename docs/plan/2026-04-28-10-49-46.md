# 详情页复制提示改为浮层

## 需求对齐

- 当前复制提示会插入详情抽屉内容流，效果差且影响布局。
- 改为不改变布局的自动消失弹框。
- 弹框显示 0.5 秒后自动消失。
- 复制 arXiv ID 时提示已复制 ID，复制标题时提示已复制标题。

## 实现计划

1. 将复制反馈持续时间从 1.6 秒调整为 0.5 秒。
2. 移除详情抽屉内容流内的复制提示节点。
3. 在页面顶层渲染固定定位 toast，避免改变抽屉布局。
4. 更新样式为轻量浮层提示。
5. 验证前端构建。
