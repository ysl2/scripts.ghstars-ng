# Papers table Published width rollback plan

## Summary

Narrow the frontend Papers table `Published` column from the current overly wide value while keeping the full header visible.

## Implementation

- Set the `Published` column preferred width to `174`.
- Bump the Papers grid persistence id to avoid stale saved widths.

## Tests

- Run frontend build.
- Run `git diff --check`.
