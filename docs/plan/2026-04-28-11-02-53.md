# 调整复制提示位置

## 需求对齐

- 保留当前复制 toast 的视觉样式。
- 将 toast 从页面全局右上角移动到详情抽屉内部右上角，靠近 Close 按钮下方。
- toast 仍然不占布局，0.5 秒自动消失。

## 实现计划

1. 将复制 toast 渲染位置移入详情抽屉节点。
2. 调整 toast CSS 为相对抽屉的绝对定位。
3. 验证前端构建和 diff whitespace。
